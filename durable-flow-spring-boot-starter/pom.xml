<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.rrobetti</groupId>
        <artifactId>durable-flow-parent</artifactId>
        <version>1.0.0-ALPHA</version>
    </parent>

    <artifactId>durable-flow-spring-boot-starter</artifactId>
    <name>Durable Flow Spring Boot Starter</name>
    <description>
        Spring Boot auto-configuration for durable-flow: wires the engine, lifecycle,
        configuration-properties binding, and the DurableFlowTemplate that automatically
        hooks afterCommit dispatch when a Spring transaction is active.
    </description>

    <properties>
        <spring-boot.version>4.0.3</spring-boot.version>
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Core durable-flow engine — always required -->
        <dependency>
            <groupId>io.github.rrobetti</groupId>
            <artifactId>durable-flow-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Spring Boot auto-configuration infrastructure -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- Spring JDBC: DataSourceUtils + TransactionSynchronizationManager -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-jdbc</artifactId>
        </dependency>

        <!-- IDE / tooling: generates spring-configuration-metadata.json for @ConfigurationProperties -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludedGroups>integration</excludedGroups>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
